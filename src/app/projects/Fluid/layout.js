export const metadata = {
  title: "Fluid Simulation | Jonatan Ebenholm",
  description: "WebGL2-based fluid simulation demo",
};

export default function ProjectLayout({ children }) {
  return children;
}
