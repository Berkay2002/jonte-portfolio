export const metadata = {
  title: "Terrain Library | Jonatan Ebenholm",
  description: "A library for procedural terrain generation",
};

export default function ProjectLayout({ children }) {
  return children;
}
