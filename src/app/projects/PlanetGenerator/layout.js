export const metadata = {
  title: "Planet Generator | Jonatan Ebenholm",
  description: "A procedural planet generation system",
};

export default function ProjectLayout({ children }) {
  return children;
}
