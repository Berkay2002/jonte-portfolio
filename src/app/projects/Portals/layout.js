export const metadata = {
  title: "Portals | Jonatan Ebenholm",
  description: "A portal rendering demonstration project",
};

export default function ProjectLayout({ children }) {
  return children;
}
